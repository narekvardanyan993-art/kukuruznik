- Не использовать автозамену по шаблону без проверки результата. После каждой правки убеждаться, что изменение попало в файл.
- Задачи вне docs/DONE.md не брать, пока версия 1.0 не закрыта. Новые идеи и фичи записывать в docs/DONE.md в раздел "1.1, после публикации".

### ЛОКАЛЬНЫЙ СЕРВЕР И ВЕРСИОНИРОВАНИЕ
- Один постоянный сервер на фиксированном порту 8080 из корня репозитория. Перед запуском убить все старые серверы (например, через `lsof -ti:8080 | xargs kill -9`).
- Сервер должен отдавать файлы без кэша (заголовки Cache-Control: no-cache, no-store), чтобы браузер не показывал старую версию.
- На КАЖДОЙ странице сайта и сцены (все .html) должна быть маленькая видимая метка «ветка + номер сборки + время» (например, <div id="build-version">), обновлять при каждой правке.
- НЕ переключать ветки в рабочей папке ради пуша журнала. Рабочая папка всегда остаётся на текущей ветке!
- Одна рабочая папка: ~/Documents/chka.am — это сам репозиторий. Постоянные worktree и копии репозитория (вроде старой ../kukuruznik-main) НЕ создавать. Если нужен main (пуш журнала, публикация беты) — временный worktree во временной папке вне репозитория, который удаляется сразу после; для беты это делает `python3 tools/publish_beta.py` сам (`--dry-run` — показать без коммита и пуша).
- Всё, что не для сайта (скачанные картинки, черновики, отбраковка, материалы будущих зданий), — только в ~/Documents/chka-kitchen (вне репозитория, в git не добавлять никогда). В репозиторий кладём только то, что нужно сайту.

### Для каждого ответа:
- в конце каждой задачи — давать отчёт для советника ОДНИМ блоком кода, начинающимся со слова ОТЧЁТ, простыми словами, без кода: что сделано, что не вышло, какие решения принял сам, что проверить глазами и где;
- ОТЧЁТ без таблицы процентов safe-change по каждому коммиту = задача не закрыта;
- в конце каждой задачи — давать две ссылки: Mac (localhost) и iPhone (адрес Mac в Wi-Fi, 192.168.x.x), каждая на отдельной строке. Ссылки должны быть всегда разные!
- отдельно, вне блока — что нужно сделать мне руками на Mac (если нужно).

### test/depth-photo — test-assets/depth.html
- Логику depth.html (шейдер, ввод, вёрстку, меню, точки-подсказки) и tools/build_frames.py, tools/color_match.py не менять без прямой просьбы. Разрешено без спроса: картинки в test-assets/frames/ + запуск `tools/build_frames.py`/`tools/color_match.py`, числа внутри CONFIG (кроме списков FRAMES/HOTSPOTS — их не подбирать на глаз) и переводы в CONFIG.I18N/UI_I18N (без добавления новых фактов о здании). Заморожено на состоянии тега depth-photo-v3.
- Формат ответа по этой ветке: 2–3 строки простыми словами → блок ОТЧЁТ одним блоком кода → вне блока что сделать руками → две ссылки (Mac localhost, iPhone 192.168.x.x) каждая на отдельной строке.
